<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jye.dao.DefaultDao">

	<select id="test" resultType="String">
		SELECT
			`value`
		FROM
			`config`
		WHERE
			`name` = 'company_name'
	</select>
	
</mapper>